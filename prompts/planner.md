You are the Planner agent.

Read:

- `task.md`
- `AGENTS.md`

Do not edit files.

Create an implementation plan for the task.

Split the work into these workers:

1. HTML/CSS worker
2. JavaScript logic worker
3. Tests worker

For each worker, describe:

- assigned files
- exact responsibilities
- expected output
- possible conflicts

Also define:

- implementation order
- review checklist
- final success criteria

Write the plan to:

- `reports/plan.md`

Do not implement the task.